lean scenario add three random items 
 to run the code 
try this command line : {
npx playwright test #all browser 
npx playwright test -project=chromium   ## for chrome
}

to get the report try this command line :{
  npx playwright show-report  
}
